# =============================================================================
# STOP — DO NOT PASTE THIS FILE IN PORTAINER
# =============================================================================
#
# This filename is kept only so old links show a warning. It cannot work in the
# Web editor (you will see: lstat .../docker: no such file or directory).
#
# Paste this file instead:
#
#   deploy/portainer/PORTAINER_STACK.yml
#
# GitHub (raw):
#   https://raw.githubusercontent.com/silvansan/HydroFoil/main/deploy/portainer/PORTAINER_STACK.yml
#
# That stack uses pre-built images (ghcr.io/silvansan/hydrofoil-*) — no build: lines.
#
# Build-from-source (git clone on server only):
#   deploy/portainer/advanced/build-from-source.compose.yml
# =============================================================================

name: wrong-compose-file

services:
  read-the-comment-above:
    image: alpine:3.20
    command:
      - sh
      - -c
      - 'echo DO NOT USE THIS FILE. Paste PORTAINER_STACK.yml from GitHub. && exit 1'
