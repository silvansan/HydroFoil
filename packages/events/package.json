{
  "name": "@hydrofoil/events",
  "version": "0.1.0",
  "description": "Domain events and event bus for HydroFoil",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hydrofoil/shared-types": "*",
    "uuid": "^11.1.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "^5.1.0"
  }
}
