{
  "name": "@hydrofoil/domain",
  "version": "0.1.0",
  "description": "Domain models and business logic for HydroFoil",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hydrofoil/shared-types": "*",
    "zod": "^3.22.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "typescript": "^5.1.0",
    "vitest": "^4.1.8"
  }
}
